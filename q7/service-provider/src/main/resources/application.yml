spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3307/week9_q7?characterEncoding=utf8
    username: root
    password: root
  aop:
    proxyTargetClass: true

dubbo:
  application:
    name: service-provider
    qosEnable: false
  registry:
    address: zookeeper://${zookeeper.address:127.0.0.1}:2181
  provider:
    token: true
